import React from "react";
import { Plane } from "../components";
import GridContainer from "./grid";

export default function PlanneContainer() {
  return (
    <Plane>
      <GridContainer />
    </Plane>
  );
}
